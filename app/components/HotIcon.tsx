import Image from "next/image";

const HotIcon = () => {
  return (
    <span className="flex justify-center items-center w-4 h-4 bg-rose-500 text-white text-xs rounded-sm">
      H
    </span>
  );
};

export default HotIcon;
